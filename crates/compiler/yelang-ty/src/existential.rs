/*! Existential predicates for trait objects (`dyn Trait`).
 *
 * Re-exports the existential predicate types defined in `crate::ty`.
 */

pub use crate::ty::{
    ExistentialPredicate, ExistentialProjection, ExistentialTraitRef,
};
