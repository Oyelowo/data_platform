but isnt it that in this language, the source usually determines the unit of stream, at least from the projection default point of view. even if we stream/batch scan and linking /grouping differently. Typically we do users[*].nested[*]
so. we are iterating over users and since downstreaming links and segments originate from `FROM`, doesnt that meant the parent and origin and should determine the unit? so we usually perhaps wait for the downstream connected links/segments be done per user? except we do an optimization for when user projects the nested related links/entity/segments be flatted e.g user[*].nested[**].{}, might mean we could optimize to already start streaming once each nested is also ready? which seem different from other query languages which seem to default to flat view from projection point of view and as such makes grouping work on any of the tables directly? so, they are flat by default then u have to manually aggregate while mine is at least auto-aggregate by default according to each user point of view.

Of course, despite my syntax/api. the internal details could work different to optimize as long as it fulfils its work semantically externally regardless of internal implementation details? or isnt it so? doesnt make sense?