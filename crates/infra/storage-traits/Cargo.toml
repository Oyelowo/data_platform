[package]
name = "storage-traits"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Public trait API for the data-platform storage layer."
license.workspace = true

[dependencies]
bytes = "1.10"
thiserror = "2.0"

[features]
default = []
async = ["dep:futures-core"]

[dependencies.futures-core]
version = "0.3"
optional = true
