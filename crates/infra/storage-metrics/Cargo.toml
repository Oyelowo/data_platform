[package]
name = "storage-metrics"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Shared metrics primitives for storage engines."
license.workspace = true

[dependencies]
parking_lot = "0.12"

[dev-dependencies]
