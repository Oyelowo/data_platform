[package]
name = "storage-filter"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Shared probabilistic filters (bloom, blocked bloom, cuckoo) for storage engines."
license.workspace = true

[dependencies]
bitvec = "1.0"
thiserror = "2.0"

[dev-dependencies]
proptest = "1.6"
